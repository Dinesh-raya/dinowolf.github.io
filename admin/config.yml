backend:
  name: github
  repo: yourusername/yourusername.github.io # Put your actual username here
  branch: main
  base_url: https://your-free-oauth-gate.com # We will link your free login key here

media_folder: "assets/images"
public_folder: "/assets/images"

collections:
  - name: "posts"
    label: "Blog Posts"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "Layout", name: "layout", widget: "hidden", default: "post" }
      - { label: "Blog Title", name: "title", widget: "string" }
      - { label: "Publish Date", name: "date", widget: "datetime" }
      - { label: "Brief Summary", name: "description", widget: "text" }
      - { label: "Post Body Content", name: "body", widget: "markdown" }